apiVersion: v1
kind: Service
metadata:
  name: {{ include "supermario-helm.fullname" . }}
  labels:
    {{- include "supermario-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "supermario-helm.selectorLabels" . | nindent 4 }}
